/**
 * @author - @ElBeenMachine
 */

import MasterPage from "@/components/Layout/Dash/DashMaster";

/**
 * Function to create the status pages dashboard page.
 *
 * @returns {ReactElement} The status pages dashboard page
 */
export default function StatusPages() {
    return <MasterPage pageTitle="Dashboard | Status Pages"></MasterPage>;
}
